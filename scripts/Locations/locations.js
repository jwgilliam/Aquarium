const locationComponent = (location) => {
  return `
  <div>
  <h2 class="location">${location.name}</h2>
  <p class="location">${location.body}</p>
</div>
              

      `
    }
    
    export default locationComponent